import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "PixelPilot AI — AI-Powered Web Design Agency",
  description:
    "Get a stunning, custom website built by AI in 24 hours. Fully automated from intake to deployment.",
  keywords: "web design, AI website, automated design, fast website",
  openGraph: {
    title: "PixelPilot AI — AI-Powered Web Design Agency",
    description: "Stunning websites, built by AI, delivered in 24 hours.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
